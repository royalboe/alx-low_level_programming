#include <stdio.h>

/**
 * main - prints decimal digits,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	while (n < 10)
	{
		printf("%d", n);
		n++
	}
	printf("\n");
	return (0);
}
